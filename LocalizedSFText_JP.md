# Localized SFText

## 目次

- [Localized SFText](#localized-sftext)
  - [目次](#目次)
  - [導入](#導入)
  - [エディタ設定](#エディタ設定)
    - [作成方法](#作成方法)
    - [基本のコンセプト](#基本のコンセプト)
      - [エディション](#エディション)
      - [ホスト・ゲスト言語](#ホストゲスト言語)
      - [言語ID](#言語id)
    - [Localized SFTextの機能](#localized-sftextの機能)
      - [エディション同期](#エディション同期)
      - [シナリオスクリプトとしてのlocalized SFText](#シナリオスクリプトとしてのlocalized-sftext)
  - [SFText多言語対応用拡張構文](#sftext多言語対応用拡張構文)
    - [スコープアノテーション](#スコープアノテーション)
    - [アノテーションの種類](#アノテーションの種類)
      - [グローバル/ローカルアノテーション（コアアノテーション）](#グローバルローカルアノテーションコアアノテーション)
      - [上方/下方束縛アノテーション](#上方下方束縛アノテーション)
      - [セリフスコープの例外的な振る舞い](#セリフスコープの例外的な振る舞い)
  - [同期](#同期)
    - [同期処理の概要](#同期処理の概要)
    - [信頼性の高い同期処理](#信頼性の高い同期処理)
    - [フォーマッタの設定](#フォーマッタの設定)
  - [Visual Studio Codeのコマンドを使用した編集](#visual-studio-codeのコマンドを使用した編集)
    - [Add Scope Annotations](#add-scope-annotations)
    - [Reallocate Scope IDs](#reallocate-scope-ids)
    - [Remove All Scope Annotations](#remove-all-scope-annotations)
    - [Switch Scope Locality, Chain Scope Upward/Downward](#switch-scope-locality-chain-scope-upwarddownward)
  - [Localized SFTextを使用した多言語実装の流れ](#localized-sftextを使用した多言語実装の流れ)
    - [ステップ1：多言語実装を開始するための最初の同期](#ステップ1多言語実装を開始するための最初の同期)
    - [ステップ2：エディションを修正・同期する](#ステップ2エディションを修正同期する)
      - [ケース1：翻訳](#ケース1翻訳)
      - [ケース2：新しい束縛スコープを追加する](#ケース2新しい束縛スコープを追加する)
      - [ケース3：新たなコアスコープを追加する](#ケース3新たなコアスコープを追加する)
      - [ケース4：既存のコアスコープを修正する](#ケース4既存のコアスコープを修正する)
    - [まとめ](#まとめ)


## 導入

多言語対応（ローカライズ）は、ユーザ自身の言語でゲームをプレイさせるための、極めて重要なプロセスです。もし世界中にゲームを提供するのであれば、各ユーザが快適にそれをプレイできるようにするため、またユーザにより良い体験を届けるため、いくつかの言語でテキストを用意することは必要不可欠です。

Localized SFTextはシナリオスクリプトの一種で、効率的に複数言語でストーリーを作成するための助けとなります。これは複数のSFTextから構成され、それぞれが特定の言語をカバーします。そして、全言語のバージョンで共通する部分の同期を可能にします。言い換えれば、あるテキストを追加もしくは修正する場合、それがすべての言語バージョンに適用されるべきものであれば、一度だけ、一つのSFTextにその変更を加えれば良いということです。

Localized SFTextを使用するために、多くのことを学ぶ必要はありません。なぜなら、既に述べられた通り、それは単純に複数のSFTextから構成されているためです。しかし同時に、同期処理による意図しない結果を避けるために、この機能に特有のキーポイントをいくつか理解する必要があります。

![](./Images/LocalizedSFText/ModifyScope_Sync.gif)

## エディタ設定

### 作成方法

新しいlocalized SFTextを作成するには、Unityエディタのプロジェクトウィンドウで右クリックし、`Create/ScenarioFlow/LocalizedSFText`を選択します。

![](./Images/LocalizedSFText/CreateNewLocalizedSFText.png)

### 基本のコンセプト

#### エディション

Localized SFTextでは、異なる言語で記述された複数のSFTextを登録します。このとき、それぞれは「**エディション**」と呼ばれます。もし英語と日本語で書かれた二つのSFTextがあるならば、それぞれ「英語エディション」、「日本語エディション」と呼ばれます。エディションは、対応するSFTextをlocalized SFTextの`Editions`プロパティに割り当てることで登録できます。

例えば、以下に示されるlocalized SFTextは、日本語、英語、簡体中国語、繫体中国語、韓国語の5つのエディションを持っています。

<table>
  <tr>
    <td><img src="./Images/LocalizedSFText/BeniSuzume_Project.png" width="100%"/></td>
    <td><img src="./Images/LocalizedSFText/BeniSuzume_Inspector.png" width="65%"/></td>
  </tr>
</table>

#### ホスト・ゲスト言語

Localized SFTextに登録されたすべてのSFTextの中で、一つ目のものは**ホスト言語**エディションとして認識されます。そして、他のものは**ゲスト言語**エディションとして認識されます。この違い（ホストかゲストか）は、同期処理の結果に影響を及ぼします。

上の例では、ホスト言語は日本語、そして他のものはすべてゲスト言語になります。

#### 言語ID

各エディションには、言語IDを割り当てる必要があります。言語IDは、各エディションがどの言語をカバーしているのかを特定するため、また、言語設定によって使用されるエディションを切り替えるために使用されます。言語ID（と対応するSFText）は、重複できません。

上の例では、5つのエディションは次のような言語IDを持っています。

+ 日本語エディション: JP
+ 英語エディション: EN
+ 簡体中国語エディション: CN-S
+ 繁体中国語エディション: CN-T
+ 韓国語エディション: KR

### Localized SFTextの機能

Localized SFTextは、以下に示される機能を持っています。

+ エディション同期
+ シナリオスクリプト

#### エディション同期

Localized SFTextのインスペクタウィンドウ上ある`Synchronize`ボタンをクリックすることで、**同期**処理を実行することができます。この処理は、全言語エディション間の全共有部分を同期します。この際、ホスト言語エディションが基準となります。詳細は後に紹介されます。

> [!NOTE]
> 上記で触れられた通り、ホスト言語エディションは同期処理においてベースラインとして扱われます。これは、同期処理の結果、ゲスト言語エディション内のスコープの内容がホスト言語エディションの内容に基づいて置き換えられ得ることを意味します。
>
> そのため、同期処理によって劇的な変化がもたらされることが予想される場合には、意図しない結果が得られた場合に備えてバックアップを作成してください。

#### シナリオスクリプトとしてのlocalized SFText

Localized SFTextは、`ScenarioScript`抽象クラスを継承する`LocalizedSFText` scriptable objectとして実装されており、一つのシナリオスクリプトとして扱うことができます。Localized SFTextは異なる言語エディションをSFTextとして保持していますが、どのSFText（言語エディション）が使用されるのかを、`LocalizedSFText`クラスの`LanguageID` 静的プロパティへ対象の言語IDを代入することで切り替えることができます。

```cs
using ScenarioFlow.Scripts;
using ScenarioFlow.Scripts.SFText.Localization;

IScenarioBookPublisher scenarioBookPublisher;
LocalizedSFText localizedSFText;
// Localized SFTextは一つのシナリオスクリプトとして扱うことができる（そうすることが実際に望ましい方法）
ScenarioScript scenarioScript = localizedSFText;
// 使用対象の言語IDを設定する必要がある
LocalizedSFText.LanguageId = "JP";
// 言語IDが"JP"であるエディションが使用される
ScenarioBook japaneseScenarioBook = scenarioBookPublisher.Publish(scenarioScript);
// 好きなタイミングで使用対象の言語IDを切り替えられる
LocalizedSFText.LanguageId = "EN";
// 言語IDが"EN"であるエディションが使用される
ScenarioBook englishScenarioBook = scenarioBookPublisher.Publish(scenarioScript);
```

> [!NOTE]
> この機能はオプションです。Localized SFTextの同期機能のみを使用するのは、全く問題がありません。使用対象の言語エディションを切り替えるために、localized SFTextをシナリオスクリプトとして使用する必要はなく、自身のプロジェクトに合った最適なローカライズの方法をとることができます。

## SFText多言語対応用拡張構文

### スコープアノテーション

元々、SFTextの拡張は縦線により、3つの領域、スコープ宣言部、コンテンツ記述部、コメント記述部に分割されていました。今、SFTextの各行は縦線により**4つの領域**に分割でき、**スコープアノテーション**を追加することができます。新しい領域はスコープアノテーション部と呼ばれます。

```
(スコープアノテーション部) | (コンテンツ記述部) | (コンテンツ記述部) | (コメント記述部)
```

スコープアノテーションは、スコープの同期処理における振る舞いを指定するために使用します。利用可能なスコープアノテーションの種類と、それらが同期においてどのように振る舞うかは後に説明されます。

Localized SFTextにエディションとして登録するために、SFTextの各スコープはスコープアノテーションを持っていなければならないことに注意してください。また、スコープアノテーションは、スコープの開始行に記述します。

アノテーションありのSFTextの構文は、アノテーションなしのSFTextの場合とほぼ同じです。明らかな違いはスコープアノテーションの有無だけであり、他の部分は完全に同一です。

### アノテーションの種類

スコープアノテーションには、以下の表に示される4つの種類があります。それぞれ、同期処理において異なる振る舞いを示します。意図しない同期結果を避けるため、これらのアノテーションの違いを理解し、適切に使い分ける必要があります。

| 名前 | 記述形式 | 概要 |
| --- | --- | --- | 
| グローバル | [ID] | スコープを**共有の要素**として宣言 |
| ローカル | (ID) | スコープを**エディション特有の要素**として宣言 |
| 上方束縛 | <↑> | スコープを**エディション特有の要素**として宣言し、上方にそれを束縛する |
| 下方束縛 | <↓> | スコープを**エディション特有の要素**として宣言し、下方にそれを束縛する |

#### グローバル/ローカルアノテーション（コアアノテーション）

グローバルそしてローカルアノテーションはコアアノテーションと呼ばれ、エディションの根幹となるスコープへ指定されます。グローバルアノテーション、ローカルアノテーションを持つスコープは、それぞれグローバルスコープ、ローカルスコープと呼ばれます。

あるスコープについて、グローバルスコープを指定するのは、全言語エディションでそのスコープの内容が共有される場合（つまり、言語エディションに関わらずスコープが同一の内容を持つ場合）です。一方、そのスコープが言語エディションによって異なる内容を持つ場合、ローカルアノテーションを指定します。

コアスコープには、スコープID（自然数）を指定することができます。スコープIDは、全言語エディション間で、どのスコープが互いに対応しているのかを特定するために使用されます。異なる言語エディションで同一のスコープIDを持つスコープがある場合、それらは対応関係のあるスコープとして認識されます。スコープIDは一つのエディションの中で重複できず、そしてゲスト言語エディションでは、各コアスコープはIDを持っていなければなりません（逆に、ホスト言語エディションの場合はコアスコープにIDを指定しないことが許されています）。

同期処理では、ホスト言語のあるスコープがグローバルアノテーションを持っている場合、対応するゲスト言語エディションのスコープは、ホスト言語のスコープで上書きされます。一方、ホスト言語のスコープがローカルである場合、上書きはされません。また、さらに重要なこととして、スコープの局所性（グローバルかローカルか）に関わらず、同期処理はすべての言語エディションが同一のスコープ構造を持つことを保証します。このスコープ構造とは、SFTextにおける"**コア**"スコープの数と、その並び順を意味します。同期処理の中で、ゲスト言語エディションのコアスコープは、それらのスコープIDと、ホスト言語エディション内の対応するスコープの順番に基づき並び替えられます。ホスト言語エディションに存在するが、言語エディションに存在しないコアスコープは、ゲスト言語エディションの適切な場所へ新たに挿入されます。ゲスト言語エディションに存在するがホスト言語エディションに存在しないコアスコープは、ゲスト言語エディションから削除されます。

#### 上方/下方束縛アノテーション

既に説明された通り、同期処理はホスト言語エディションを基準として、すべての言語エディションが同一のスコープ構造を持つことを保証します。これは、例えば、同期処理の後では、もしホスト言語エディションがスコープID"1"のスコープを持っているのならゲスト言語エディションにもスコープID"1"のついたスコープがあり、もしホスト言語エディションにスコープID"2"のスコープがないのならスコープID"2"を持つスコープはどのゲスト言語エディションにも存在しないということを意味します。

上方・下方束縛アノテーションは、スコープ構造を変更しないスコープを定義するために使用されます。上方もしくは下方束縛アノテーションを持つスコープはそれぞれ上方束縛スコープ、下方束縛スコープと呼ばれ、それらをまとめて束縛スコープと呼びます。

上方もしくは下方束縛アノテーションを持つ束縛スコープは、上もしくは下にあるコアスコープへ束縛されます。束縛スコープは寄生的なスコープとして扱われ、エディションのスコープ構造に影響を与えません（スコープ構造の定義を思い出してください）。より具体的には、これら束縛スコープは、エディションが他のエディションには出現しない、特有のコンテンツを持つ場合に有用です。例えば、特定の言語エディションにコメントを残したい場合、もしくは言語特有のセリフ（もしくはコマンド）が必要な場合、束縛スコープを宣言することになるかもしれません。

ホストエディションにある束縛スコープは、同期処理により他のゲスト言語エディションに現れません。一方で、ゲスト言語エディションに存在する束縛スコープについて、ホスト言語エディションの中に対応するコアのスコープ（束縛スコープと紐づいているコアスコープ）が存在しなくなった場合、それは削除されます。また、ゲスト言語エディションに存在する束縛スコープは、それらのコアスコープに従って順番が並び替えられます。

#### セリフスコープの例外的な振る舞い

コアアノテーションのついたセリフスコープは、同期処理の中で、他の種類のスコープとは異なる振る舞いを示します。具体的には、セリフスコープは次の二つの規則に従います。

+ セリフスコープのセリフ部分は、同期の中でローカルスコープのように振る舞う
+ セリフスコープの追加パラメータ部分は、同期の中で、そのアノテーションの種類によって振る舞いを変える

まず、セリフスコープのセリフ部分はそのスコープにグローバルアノテーションが割り当てられていたとしても、同期処理の中で、ホスト言語エディションによる影響を受けません。「セリフ部分」は、セリフと、話者名を含むすべての行を含みます。

次に、セリフスコープの追加パラメータ部分は、そのスコープにグローバルアノテーションが指定されていた場合、同期処理の中でホスト言語エディションに基づき修正される可能性があります。しかし、そうでなければ修正されることはありません。「追加パラメータ部分」とは、"-->"で始まる、セリフスコープ内のすべての追加パラメータ行を含みます。

以下に、2つの例を示します。まず、二つのスコープ内のセリフ部分は「ローカルな」振る舞いを示します。一方で、それらのスコープ内の追加パラメータ部分は異なる振る舞いを示します。一つ目は、グローバルアノテーションが対応するスコープに指定されているため「グローバルな」振る舞いを、二つ目は、ローカルアノテーションが指定されているため「ローカルな」振る舞いを示します。

```
[1] | Sheena | Hello!                | セリフ部分 (「ローカルな」振る舞いを示す)
    |        | How are you doing?    | セリフ部分
    |        | --> {Arg1} {Arg2}     | 追加パラメータ部分 (「グローバルな」振る舞いを示す)
    |        | --> {Arg3}            | 追加パラメータ部分
(2) | Rio    | Hello.                | セリフ部分 (「ローカルな」振る舞いを示す)
    |        | Not bad.              | セリフ部分
    |        | --> {Arg1} {Arg2}     | 追加パラメータ部分 (「ローカルな」振る舞いを示す)
    |        | --> {Arg3}            | 追加パラメータ部分
```

> [!TIP]
> セリフスコープの振る舞いが、少々複雑に感じたかもしれません。この、上記で説明された、少し込み入った振る舞いはセリフスコープが持つ二つの相反する要求に由来します。
>
> 第一に、話者名とセリフは、エディションがカバーする言語によって常に異なります。そのため、これらの要素は「ローカルな」特性を持っていなければなりません。しかし、同時に、セリフスコープの追加パラメータは言語特有の要素になりにくいため、ほとんどの場合は言語に関わらず同一の値となります。そのため、これらの要素はほとんどの場合、「グローバルな」特性を持つべきです。
>
> これらの要求に基づき、セリフスコープのセリフ部分はそのアノテーションの種類に関わらず「ローカルな」特性を持つように、追加パラメータ部分については「可変な」特性を持つようにデザインされています。追加パラメータ部分の「可変な」特性の理由は、基本的に追加パラメータは言語非依存で同一の値を持つべきである一方で、時々言語によって異なる値が必要なることもあるかもしれないためです。

## 同期

### 同期処理の概要

同期処理により、localized SFTextの各言語エディションに何が起こるのかを以下にまとめます。

+ 各ゲスト言語エディションの各グローバルスコープが、ホストエディション内で同一のスコープIDを持つ、対応したグローバルスコープで置き換えられる
+ 各ゲスト言語エディションの各コアスコープについて、それに対応する、同一のスコープIDを持つスコープがホスト言語エディションに存在しなければ、それは取り除かれる
+ ホスト言語エディションの各コアスコープについて、それがゲスト言語エディションに存在しなければ、適切な位置に挿入される
+ ゲスト言語のスコープが、ホスト言語エディションのスコープ順序に基づいて並び替えられる。束縛スコープは、それらのコアスコープに従って並び替えられる
+ 各言語エディション（ホスト言語エディションを含む）のスコープに、連番でスコープIDが再割り当てされる

同期時のエラーを避けるため、次のルールに従ってください。

+ エディションとして登録されるSFTextと、それらの言語IDは唯一である
+ 全エディションの全スコープはスコープアノテーションを持っている
+ スコープIDが、一つのエディションの中では唯一のものである
+ スコープIDが、すべてのゲスト言語エディションのすべてのコアスコープに割り当てられている
  + ホスト言語エディションにおいては、スコープIDの省略が許される。ただし、ホスト言語エディション内の、スコープIDのないスコープは、そのスコープが他のどのゲスト言語エディションにも存在しないことを表していることに注意

### 信頼性の高い同期処理

Localized SFTextの同期機能は、異なる言語エディション間で共有されるコンテンツの同期を可能にし、多言語対応にとても有用です。しかし、同時に、この同期処理は破壊的な処理であり、よって危険も孕んでいます。スコープアノテーションが適切に指定されていない場合、ゲスト言語エディションが同期の結果、台無しになるかもしれません。そのため、そのような残念な状況を避けるため、下記の方針に従う必要があります。

+ ホスト言語エディションについて：
  + スコープIDを直接変更しない。スコープIDの再割り当ては同期処理に任せることができる
+ ゲスト言語エディションについて：
  + 既存のスコープIDに触れない
  + グローバルもしくはローカルアノテーションを持つ新たなスコープを追加したり、あるいはそのような既存のスコープを削除しない（束縛スコープの追加並びに削除は問題ない）

### フォーマッタの設定

同期処理は、エディションの内容を同期するだけでなく、テキストの整形も行います。この整形は、VSCode拡張機能のSFTextフォーマッタが行うものと同一の処理です。同期におけるフォーマット処理は、VSCodeのフォーマッタ同様、整形のために半角文字と全角文字を区別します。そのため、適切な整形のためにはいくつかの設定を修正しなければならないかもしれません。

フォーマッタの設定は、Unityエディタの上部メニューで`Window/ScenarioFlow/Localized SFText Settings`を選択することで開くことができます。以下に示される三つの項目は、VSCodeのフォーマッタのものと同一です。それらの設定について詳しくは、[SFTextフォーマッタ](./SFText_JP.md#sftextフォーマッタ)を参照してください。

![](./Images/LocalizedSFText/FormatterSettings.png)


## Visual Studio Codeのコマンドを使用した編集

Visual Studio Codeの拡張機能、SFText Utility (バージョン1.2.0以降)は、スコープアノテーションを操作するためのコマンドをいくつか提供しています。スコープアノテーションを手動で編集する必要はなく、効率化のため、それらのコマンドを使用することができます（むしろするべき）。

利用可能なコマンドを、以下にまとめます。

| コマンド | ショートカットキー (Windows) | 機能 |
| --- | --- | --- |
| Add Scope Annotations | - | スコープアノテーションを持たないすべてのスコープにスコープアノテーションを追加する |
| Reallocate Scope IDs | - | コアスコープに連番でスコープIDを割り当てる |
| Remove All Scope Annotations | - | SFTextのスコープアノテーション部を取り除く |
| Switch Scope Locality | F4 | スコープの局所性（グローバル・ローカル）を切り替える |
| Chain Scope Upward | Ctrl+Shift+Up | スコープアノテーションを上方束縛アノテーションで置き換える |
| Chain Scope Downward | Ctrl+Shift+Down | スコープアノテーションを下方束縛アノテーションで置き換える |

### Add Scope Annotations

このコマンドは、スコープアノテーションを持たないスコープへスコープアノテーションを追加します。これは、二つのケースで使用されます。

一つ目は、ローカライズされていないSFTextを、エディションとして初期化する場合です。このケースでは、３つの領域を持つすべての行が、スコープアノテーションを含む４番目の領域（つまり、スコープアノテーション部）を獲得します。

![](./Images/LocalizedSFText/CmdAddScopeAnnotationsCase1.gif)

二つ目のケースは、既存のエディションに対し、スコープを追加する時です。追加されたスコープに、自動でスコープアノテーションを追加することができます。

![](./Images/LocalizedSFText/CmdAddScopeAnnotationsCase2.gif)

どの種類のスコープにどの種類のスコープアノテーションが割り当てられるのかは、設定で変更できます。デフォルトでは、すべての種類のスコープにグローバルアノテーションが割り当てられるようになっています。

![](./Images/LocalizedSFText/CmdScopeAnnotationSettings.png)

> [!NOTE]
> `Add Scope Annotations`コマンドは、「粗い」スコープアノテーションの割り当てに使用します。「細かい」割り当てのためには、他の`Switch Scope Locality`、`Chain Scope Upward/Downward`コマンドを使用してください。

### Reallocate Scope IDs

このコマンドは、コアスコープへ連番のスコープIDを割り当てます。この処理は、既存のスコープIDを上書きしますが、上方・下方束縛スコープには影響しないことに注意してください。

また、このコマンドはあまり使われないかもしれません。スコープIDは同期処理によって自動で割り振られ、スコープIDの一貫性を保つためには、それらを手動で割り当てるべきでないためです。このコマンドが使用され得る１つのケースは、既存のスコープIDを誤って削除してしまい、それを復元するのが困難な場合です。そのような場合は、スコープIDの再割り当てにこのコマンドを使用した方がいいかもしれません。

![](./Images/LocalizedSFText/CmdReallocateScopeIds.gif)

> [!NOTE]
> `Reallocate Scope IDs`を呼び出す前に、`Add Scope Annotations` コマンドを呼び出しておき、すべてのスコープがアノテーションを持っていることを保証するべきです。`Reallocate Scope IDs` コマンドは、スコープアノテーションを持たないスコープを無視します。

### Remove All Scope Annotations

このコマンドは、SFText内のすべてのスコープアノテーションを削除します。このコマンドは、エディションをリセットしたい場合に使用されるかもしれません。

![](./Images/LocalizedSFText/CmdRemoveAllAnnotations.gif)

### Switch Scope Locality, Chain Scope Upward/Downward

これらのコマンドを使用し、スコープの粒度でアノテーションを変更することができます。これらのコマンドで、選択範囲の中のスコープの、スコープアノテーションの種類を切り替えます。そして、これらのコマンドはショートカットキー（`Switch Scope Locality` は`F4`、`Chain Scope Upward/Downward` はそれぞれ`Ctrl+Shift+Up/Down`）により素早く呼び出すことができます。

![](./Images/LocalizedSFText/CmdSwitchScopeAnnotation.gif)

## Localized SFTextを使用した多言語実装の流れ

このセクションでは、localized SFTextを使用した多言語実装の、標準的な流れを見ていきます。基本的な戦略を簡潔に確認した後、具体例と共にそれを試します。

始めに、localized SFTextを使用した多言語実装で行うことは、大まかに以下の二つです。

+ ホスト言語エディションにグローバルな変更を加え、その変更を他のゲスト言語エディションに反映させるため、同期を実行する
+ ローカルな変更を対象の言語エディションに直接加える

「ローカルな」とは、ある特定の言語エディションには必要であるが、他のどの言語エディションにも必要ない変更のことです。「グローバルな」とは、すべての言語エディションの間で平等に必要な、同一の変更のことです。例えば、（グローバルアノテーションを持つ）グローバルスコープの修正はグローバルな変更で、（ローカルアノテーションを持つ）ローカルスコープの修正はローカルな変更です。また、コアスコープを追加または削除するのはグローバルな変更で、（上方・下方束縛アノテーションを持つ）束縛スコープに関する操作はローカルな変更です。

より具体的には、グローバルな変更のためには以下の手順を踏みます。

1. ホスト言語エディションに変更を加える
2. VSCodeのコマンドによりスコープアノテーションを追加し、必要に応じて手動で修正を加える
3. エディションを同期する

スコープIDを自身で変更することはなく、その割り当て・再割り当ては同期処理に任せることができる（任せなければならない）ことに注意してください。

では、多言語実装のワークフローを実際に体験してみます。ここでは、例として[紅すずめ](https://www.aozora.gr.jp/cards/001475/card51065.html)を利用します。紅すずめは日本の文学作品で、パブリックドメインとして[青空文庫](https://www.aozora.gr.jp/)でアクセス可能です。

目標は、多言語実装の始め方を理解し、同期処理の振る舞いを理解することです。具体的に、これより行うのは以下のことです。

+ 多言語実装の環境を整える
+ いくつかの変更を加え、同期を試す

> [!NOTE]
> テキストについて、スペースの都合上全文は示さず、一部のみを掲載します。ベースラインとなるテキスト（下記の「ケース1」内での、翻訳済みのエディション）の全文は、[ここ](./Scripts/)で閲覧可能です。

### ステップ1：多言語実装を開始するための最初の同期

Localized SFTextを使用した多言語実装の環境を整えるためには、まずはベースとなる言語エディションの作成から始めます。今回は、元々のテキストが日本語で書かれているため、日本語をベースとします。

![](./Images/LocalizedSFText/BeniSuzume_JP_Baseline.png)

この時点では、必要なすべてのセリフ、マクロ、コマンドスコープを含んだ完全なものを用意する必要はないことに注意してください。ここでは、セリフスコープをいくつかと、最低限必要なマクロスコープだけ用意すれば問題ありません。

次に、`Add Scope Annotations` コマンドを呼び出して、スコープアノテーションを追加します。

![](./Images/LocalizedSFText/BeniSuzume_JP_AddAnnotation.png)

続けて、他の言語エディションのベースを作成します。Unityエディタでlocalized SFTextを作成し、必要な言語エディションを登録します。今回の例では、英語、簡体中国語、繫体中国語、そして韓国語エディションをゲスト言語として用意します。この時点では、ベースラインのエディション以外はすべて、空であることに注意してください。

<table>
  <tr>
    <td><img src="./Images/LocalizedSFText/BeniSuzume_Project.png" width="100%"/></td>
    <td><img src="./Images/LocalizedSFText/BeniSuzume_Inspector.png" width="65%"/></td>
  </tr>
</table>

ここで、localized SFTextのインスペクタウィンドウ上にある`Synchronize` ボタンを押し、同期処理を実行します。同期処理の後、ホスト言語エディション（日本語エディション）が他のすべてのゲスト言語エディションにコピーされているのが確認できます。

![](./Images/LocalizedSFText/FirstSync.gif)

### ステップ2：エディションを修正・同期する

#### ケース1：翻訳

この時点で、多言語実装を進める準備ができています。ここで、エディションにいくつかの変更を加え、必要であれば同期を実行します。

始めに、ベースラインの言語で書かれたコピー済みのテキストを、各エディションがカバーする言語へ翻訳しましょう。これは、必要に応じてスコープ一つ一つを修正することによって行います。

英語:

![](./Images/LocalizedSFText/BeniSuzume_EN_Translation.png)

簡体中国語:

![](./Images/LocalizedSFText/BeniSuzume_CN-S_Translation.png)

繁体中国語:

![](./Images/LocalizedSFText/BeniSuzume_CN-T_Translation.png)

韓国語:

![](./Images/LocalizedSFText/BeniSuzume_KR_Translation.png)

この修正では、エディションへローカルな変更しか加えていません。そのため、同期処理は必要ありません。

> [!NOTE]
> セリフスコープのセリフ部分は、スコープがグローバルアノテーションを持っていたとしても、必ず「ローカルな」特性を持つことを思い出してください。セリフスコープのスコープアノテーションは、追加パラメータ部分の局所性にのみ影響します。

#### ケース2：新しい束縛スコープを追加する

一部エディションに、束縛スコープを追加してみます。以下の通り、英語および韓国語エディションへ変更を加えます。

英語: 

![](./Images/LocalizedSFText/BeniSuzume_EN_NewChained.png)

韓国語:

![](./Images/LocalizedSFText/BeniSuzume_KR_NewChained.png)

英語エディションに上方束縛スコープを、韓国語エディションに下方束縛スコープを追加しました。これらの変更は他の言語エディションに影響を及ぼさないため、エディションを同期する必要はありません。

#### ケース3：新たなコアスコープを追加する

次に、いくつかのグローバルスコープとローカルスコープを追加します。この場合は、全言語エディションに存在するべきであるものの、現時点ではそうなっていない新たなスコープを追加しようとしています。そのため、これはグローバルな編集であり、同期が必要になります。

始めに、必要な変更をホスト言語エディションへ加えます。このケースでは、日本語エディションです。

日本語:

![](./Images/LocalizedSFText/BeniSuzume_JP_NewCoreScopes.png)

ここで、新しいコアスコープへスコープアノテーションを追加する必要がありますが、スコープIDは付与しなくてよい（むしろするべきではない）ことに注意してください。そうでなければ、エディション間のスコープIDの一貫性が破壊されてしまうかもしれません。スコープIDの割り当ては、同期処理に任せることができます。

> [!TIP]
> ホスト言語エディションを切り替えるのは、全く問題がありません。元々のテキストがどの言語であったかに関わらず、どのエディションをホストにするのかを決めることができます。この例では、元々のテキストは日本語でしたが、最初の同期の後、例えば英語エディションをホスト言語として使用し始めることができます。そのためには、localized SFTextのエディションの登録順を変更し、対象のエディションを1番目に設定します。
>
> 一貫性を保つため、ホスト言語の切り替えは同期処理の後に行うべきであることに注意してください。

編集を終えたら、localized SFTextのインスペクタウィンドウ上にある`Synchronization`ボタンを押して、同期処理を実行します。


![](./Images/LocalizedSFText/AddCoreScope_Sync.gif)

同期処理の結果は以下の通りになります。

日本語:

![](./Images/LocalizedSFText/BeniSuzume_JP_NewCoreScopes_Sync.png)

英語:

![](./Images/LocalizedSFText/BeniSuzume_EN_NewCoreScopes_Sync.png)

簡体中国語:

![](./Images/LocalizedSFText/BeniSuzume_CN-S_NewCoreScopes_Sync.png)

繁体中国語:

![](./Images/LocalizedSFText/BeniSuzume_CN-T_NewCoreScopes_Sync.png)

韓国語:

![](./Images/LocalizedSFText/BeniSuzume_KR_NewCoreScopes_Sync.png)

ポイントは以下の通りです。

+ すべてのスコープが新しいスコープIDを獲得した
+ 新しいスコープ（新しい追加会話パラメータ部分を含む）がゲスト言語エディションに追加された

一つ目の点について、これが、手動でスコープIDを割り当てる必要がない理由です。同期処理はその処理の中で、自動で新しいスコープIDを適切に割り当てます。

二つ目の点について、最も重要なのは、スコープID 6を持つ新しいスコープが、エディションによって異なる位置へ挿入されたということです。英語エディションでは上方束縛スコープの下に挿入されているのに対して、韓国語エディションでは下方束縛スコープの上に挿入されています。これらの結果は、上方束縛スコープと下方束縛スコープの違いを明確に示しています。

束縛スコープは、その束縛スコープと束縛先のスコープの間に、いかなる追加のスコープも入り込まないことを保証します。より具体的には、英語エディションでは、上方束縛コメントスコープは、スコープID 5のマクロスコープへ束縛されているため、いかなるスコープも、同期処理の結果によってそれら二つのスコープの間へ挿入されません。一方で、韓国語エディションでは、下方束縛コメントスコープがスコープID 7のセリフスコープへ束縛されているため、それら二つのスコープの間に、同期処理の結果によっていかなるスコープも入り込みません。

> [!TIP]
> このケースでは、新しいコアスコープの追加を試みました。しかし、既存のコアスコープを削除をする場合も、同じ方法で行うことができます。その場合、ホスト言語エディションからまずはスコープを取り除き、同期処理を実行します。

#### ケース4：既存のコアスコープを修正する

ここでは、エディション内にあるいくつかの既存のコアスコープへ変更を加えます。これは全体へ影響を及ぼす変更を含むので、変更の後に同期処理が必要です。

まず、いくつかのスコープに、以下のような変更を加えます。

日本語:

![](./Images/LocalizedSFText/BeniSuzume_JP_ModifyScopes.png)

英語:

![](./Images/LocalizedSFText/BeniSuzume_EN_ModifyScopes.png)

日本語エディションでは、次のスコープIDを持つスコープに変更を加えました。

+ スコープID 1 (テキスト修正)
+ スコープID 5 (コメント追加)
+ スコープID 6 (パラメータ修正)
+ スコープID 8 (パラメータ修正)
+ スコープID 9 (パラメータ修正)

英語エディションでは、次のスコープIDを持つスコープに変更を加えました。

+ スコープID 1 (テキスト修正)

次に、エディションを同期します。

![](./Images/LocalizedSFText/ModifyScope_Sync.gif)

結果は以下の通りになります。

日本語:

![](./Images/LocalizedSFText/BeniSuzume_JP_ModifyScopes_Sync.png)

英語:

![](./Images/LocalizedSFText/BeniSuzume_EN_ModifyScopes_Sync.png)

簡体中国語:

![](./Images/LocalizedSFText/BeniSuzume_CN-S_ModifyScopes_Sync.png)

繁体中国語:

![](./Images/LocalizedSFText/BeniSuzume_CN-T_ModifyScopes_Sync.png)

韓国語:

![](./Images/LocalizedSFText/BeniSuzume_KR_ModifyScopes_Sync.png)

ポイントは以下の通りです。

+ エディション内のローカルスコープ（スコープID 1）は独立した値を持っている
+ ゲスト言語エディション内のグローバルスコープ（追加会話パラメータ部分を含む）は、ホスト言語エディション内の対応するもので置き換えられた

まず、ゲスト言語エディション内の、スコープID 1のローカルスコープは、ホスト言語エディションの対応するスコープを上書きしたにもかかわらず、同期処理の結果上書きされていません。これは、ローカルアノテーションの特性をはっきりと示しています。ローカルスコープは、個々の言語で独立な内容を持つことができ、同期処理によって上書きされません。

他方で、グローバルスコープは反対の振る舞いを示します。ゲスト言語エディション内のすべてのグローバルスコープは、ホスト言語エディション内に存在する、同じスコープIDを持つ、対応したスコープで置き換えられます。

> [!TIP]
>
> 同期処理は、スコープの要素を細かくは区別しません。エディションを、スコープの粒度で解析します。そのため、ゲスト言語エディション内のグローバルスコープはその全体が置き換えられます。例外はセリフスコープで、これは、セリフ部分と追加パラメータ部分を分けて解析されます。

### まとめ

ここでは、localized SFTextを使用した多言語実装の標準的な流れを確認しました。最も重要なことは、加えようとしている変更が全体的に影響を与えるのか、単に局所的な影響を与えるのかを判断するということです。もしその変更がすべてのエディションへ影響を与えるのであれば、まずはその変更をホスト言語エディションへ加え、そして同期処理を実行する必要があります。そうでなければ、同期処理は必要なく、単に対象の言語エディションに直接変更を加えるだけで済みます。